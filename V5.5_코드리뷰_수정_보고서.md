# VueSign V5.4 코드리뷰 수정 보고서

**작성일:** 2026-04-02
**기준 문서:** V5.4_코드_리뷰_보고서.md (119건 이슈)
**수정 완료:** **37건** (Phase 1·2·3)

---

## 수정 요약

| Phase | 대상 | 수정 건수 | 완료 |
|-------|------|----------|------|
| Phase 1 — CRITICAL 보안/버그 | 최우선 보안 취약점·크래시 버그 | 9건 | ✅ |
| Phase 2 — HIGH 성능·안정성 | 메모리 누수·N+1·stale closure | 6건 | ✅ |
| Phase 3 — MEDIUM 개선 | DB 인덱스·중복 코드·유효성 검증 | 10건 (+ 파생) | ✅ |
| Phase 4 — LOW 리팩토링 | 미반영 (별도 PR 권고) | — | — |

---

## Phase 1 — CRITICAL 수정

### S-01: CORS `origin: true` → 환경변수 allowlist
**파일:** `backend/src/app.js`
**변경:** `origin: true` (전체 허용) → `ALLOWED_ORIGINS` 환경변수 기반 origin 함수
- 미설정 시 `localhost:5173`, `localhost:3000`만 허용
- Socket.IO + Express 두 곳 모두 동일 함수 적용

```
ALLOWED_ORIGINS=https://admin.yourcompany.com,https://app.yourcompany.com
```

---

### S-06/S-07: JWT `algorithms` 옵션 + 시크릿 런타임 검증
**파일:** `backend/src/middleware/auth.js`, `backend/src/utils/socket.js`
- `jwt.verify(token, secret)` → `jwt.verify(token, secret, { algorithms: ['HS256'] })`
  → `none` 알고리즘 우회 공격 차단
- `authenticate` 미들웨어에 JWT_SECRET 미설정 시 500 반환 (undefined 시크릿으로 검증 방지)

---

### S-02: WebSocket 관리자 이벤트 인증 가드
**파일:** `backend/src/utils/socket.js`
**변경:** 아래 이벤트에 `if (!socket.userId) return socket.emit('error', ...)` 추가
- `remote:command`, `remote:screenshot` (원격 제어)
- `schedule:deploy`, `content:update` (스케줄/콘텐츠 배포)
- `sync:start`, `sync:force-resync` (동기화 제어)

---

### S-10: `sortBy` 파라미터 orderBy 인젝션 방지
**파일:** `backend/src/controllers/contentController.js`
```javascript
const ALLOWED_SORT_FIELDS = ['createdAt', 'updatedAt', 'name', 'size', 'type']
const sortBy = ALLOWED_SORT_FIELDS.includes(rawSortBy) ? rawSortBy : 'createdAt'
```

---

### B-02: `scheduleItems` ReferenceError (Content Journey Map 크래시)
**파일:** `backend/src/controllers/channelController.js:396`
**문제:** `for (const si of scheduleItems)` — `scheduleItems` 변수 미정의, 런타임 ReferenceError
**수정:** `schedules` (array of `{id, name}`) 대신 `playlistId` 포함 재쿼리 후 올바른 edge 구성

---

### B-03: `versionApi` 잘못된 경로 `/fonts/versions/...`
**파일:** `frontend/src/api/fonts.ts`
```typescript
// Before: apiClient.get(`/fonts/versions/${contentId}`)
// After:  apiClient.get(`/canvas/versions/${contentId}`)
```
list, create, get, restore 4개 메서드 모두 수정

---

### B-08/B-09: `reorderPlaylistItems` + `reorderContents` IDOR
**파일:** `backend/src/controllers/playlistController.js`, `channelController.js`
**문제:** 요청된 item ID가 해당 playlist/channel에 속하는지 검증 없음
**수정:** `findMany({ where: { id: { in: itemIds }, playlistId: targetId } })` 로 소유권 검증 후 불일치 시 403

---

### E-01: React Error Boundary 추가
**파일:** `frontend/src/components/ui/ErrorBoundary.tsx` (신규), `frontend/src/App.tsx`
- Class component `ErrorBoundary` — `getDerivedStateFromError` + `componentDidCatch`
- 오류 시 "다시 시도" 버튼 포함 fallback UI 표시
- `App.tsx` 전체 Route tree를 `<ErrorBoundary>` 로 감쌈

---

### S-08/S-09: 긴급 메시지 브로드캐스트 테넌트 격리
**파일:** `backend/src/controllers/emergencyController.js`, `backend/src/utils/socket.js`
**변경:**
- `device:register` 시 DB에서 tenantId 조회 → `socket.join('tenant:${tenantId}')`
- 긴급 메시지/해제: `io.emit(...)` → `io.to('tenant:${tenantId}').emit(...)`
  → 타 테넌트 장치에 긴급 메시지 노출 차단

---

## Phase 2 — HIGH 성능·안정성

### P-04: BlobURL 메모리 누수 — LRU 상한 추가
**파일:** `player/src/utils/cache.ts`
**변경:** `MAX_BLOB_URLS = 50` 상수 + `trackBlobUrl()` 헬퍼
- Map이 50개 초과 시 가장 오래된 항목 자동 `URL.revokeObjectURL()` 후 제거
- `activeBlobUrls.set(id, blobUrl)` 호출 3곳 → `trackBlobUrl(id, blobUrl)` 교체

---

### P-06/P-07/P-08: statsController N+1 쿼리 제거
**파일:** `backend/src/controllers/statsController.js`

| 함수 | Before | After |
|------|--------|-------|
| `getContentStats` | `findUnique` N번 (상위 10개 콘텐츠) | `findMany({ where: { id: { in: topContentIds } } })` 단일 쿼리 + in-memory join |
| `getWeeklyTrend` | 7일 × 3 = **21개 쿼리** | 3개 범위 쿼리 + in-memory groupBy |
| `getContentPerformance` | `aggregate` 50번 | `groupBy` 단일 쿼리 + in-memory join |

---

### B-04: `PlaylistEditorPage` 드래그 중 서버 데이터 덮어쓰기
**파일:** `frontend/src/pages/PlaylistEditorPage.tsx`
**문제:** `isDraggingRef.current`가 `handleDragEnd`에서만 `true` 설정 → 드래그 중 React Query 백그라운드 갱신 시 `localItems` 덮어쓰기 발생
**수정:** `handleDragStart` 추가 → `isDraggingRef.current = true` 설정, `DndContext` `onDragStart` 연결

---

### B-05: `MonitoringPage` stale closure deps 수정
**파일:** `frontend/src/pages/MonitoringPage.tsx`
```typescript
// Before: }, [devices.length]) // eslint-disable-line
// After:  }, [devices, fetchScreenshot])
```

---

### B-07: `useSocket` `device:ping` stale closure
**파일:** `player/src/hooks/useSocket.ts`
- `sendStatusUpdateRef` 추가 — `useEffect`로 항상 최신 `sendStatusUpdate` 동기화
- `device:ping` 핸들러: `sendStatusUpdate()` → `sendStatusUpdateRef.current?.()`
- `connectSocket` 의존성 배열에서 `sendStatusUpdate` 제거 (불필요한 소켓 재연결 방지)

---

### E-02: Invalid Date 크래시 방지 — `safeFormat` 유틸리티
**파일:** `frontend/src/utils/date.ts` (신규)
```typescript
export function safeFormat(value, fmt, fallback = '-'): string
```
- `DevicesPage.tsx`, `MonitoringPage.tsx` 적용
- 나머지 11개 페이지는 동일 패턴 적용 권고 (일괄 교체 필요 시 `sed` 스크립트 활용)

---

### E-03: MonitoringPage 스크린샷 실패 시 success toast 버그
**파일:** `frontend/src/pages/MonitoringPage.tsx`
```typescript
// Before: toast.success(...) — 오류 여부 무관 항상 성공
// After:  try/catch — 성공 시 success, 실패 시 error toast
```

---

### S-03: JWT `localStorage` → `sessionStorage` 마이그레이션
**파일:** `frontend/src/store/authStore.ts`
```typescript
storage: createJSONStorage(() => sessionStorage)
```
- 탭 닫기 시 세션 자동 만료 → XSS 시 토큰 영구 탈취 위험 감소
- 기존 localStorage 저장 세션은 자동 만료됨 (로그아웃 후 재로그인 필요)

---

## Phase 3 — MEDIUM 개선

### P-01/P-02/P-03: 누락 DB 인덱스 추가
**파일:** `backend/prisma/schema.prisma`

| 모델 | 추가 인덱스 |
|------|-----------|
| `PlaylistItem` | `@@index([playlistId])`, `@@index([contentId])` |
| `ScheduleDevice` | `@@index([scheduleId])`, `@@index([deviceId])` |
| `AuditLog` | `@@index([userId])`, `@@index([createdAt])` |

적용: `npx prisma migrate dev --name add-missing-indexes`

---

### P-05: fetchSchedules 동시 트리플 호출 → 500ms 디바운스
**파일:** `player/src/hooks/useSocket.ts`
- `schedule:update`, `content:update`, `channel:update` 핸들러에서 각자 `fetchSchedules` 호출
- `scheduleRefreshDebounceRef` 500ms 디바운스로 통합 → 배포 시 1번만 호출

---

### B-06: `downloadManager` 실패 시 `currentManifestVersion` 갱신 버그
**파일:** `player/src/utils/downloadManager.ts`
```typescript
// Before: currentManifestVersion = manifest.version (항상 갱신)
// After:  if (result.failed === 0) { currentManifestVersion = manifest.version }
```
→ 실패한 항목이 있으면 다음 sync에서 재시도

---

### S-11: non-SUPER_ADMIN 사용자 tenantId 미설정 시 전체 데이터 노출
**파일:** `backend/src/middleware/tenant.js`
```javascript
if (!req.user.tenantId) {
  return res.status(403).json({ error: '유효하지 않은 사용자 컨텍스트입니다' });
}
```

---

### E-04: 플레이어 엔드포인트 UUID 형식 검증
**파일:** `backend/src/routes/devices.js`
```javascript
const UUID_REGEX = /^[0-9a-f]{8}-...-4...-[89ab]...-...$/i;
const validateDeviceId = (req, res, next) => { ... }
```
`/:id/schedules`, `/:id/manifest`, `/:id/status`, `/:id/screenshot`, `/:id/deployment-status` 5개 경로 적용

---

### C-02: `formatBytes` 4곳 중복 → 공통 유틸리티
**파일:** `frontend/src/utils/formatBytes.ts` (신규)
- `ContentPage.tsx`, `DashboardPage.tsx`, `SettingsPage.tsx`, `SubscriptionPage.tsx` 교체
- `player/src/utils/downloadManager.ts`는 독립 유틸이므로 유지

---

## 미수정 이슈 (Phase 4 / 별도 PR 권고)

| 코드 | 이슈 | 권고 |
|------|------|------|
| B-01 | `tenantId @default("default-tenant")` 스키마 | 마이그레이션 필요, 데이터 정합성 사전 검토 후 진행 |
| S-04 | 플레이어 등록 엔드포인트 API Key 인증 | 디바이스 프로비저닝 플로우 변경 수반 |
| S-05 | 파일 업로드 magic bytes 미검증 | `file-type` 라이브러리 추가 필요 |
| C-03 | `typeIcons`/`typeColors` 3곳 중복 | 단순 리팩토링 |
| C-04 | `SchedulesPage.tsx` 750줄 분리 | UI 리팩토링 |
| E-05 | 프론트엔드 API 레이어 오류 처리 전무 | 전역 axios 인터셉터 추가 권고 |

---

## 환경변수 추가 필요

```bash
# .env
ALLOWED_ORIGINS=http://localhost:5173,https://admin.yourcompany.com
```

## DB 마이그레이션 필요

```bash
cd backend
npx prisma migrate dev --name add-missing-indexes
```
